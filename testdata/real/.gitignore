# Real show files carry designer names, venues and console IP/MAC addresses.
# They are ignored by default; commit one deliberately with `git add -f` or an
# exception below.
*.tmix
!kitchen_sink.tmix
